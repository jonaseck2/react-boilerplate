/** @jsx React.DOM */

var React = require('react');

var APP = React.createClass({

        render: function() {
            return(
                <h1>React boilerplate</h1>
            );
        }
    });

module.exports = APP;